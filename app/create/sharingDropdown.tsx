// import { useState } from 'react';
// import { ComboboxItem, Select } from '@mantine/core';
// import { useLocalStorage } from 'usehooks-ts';

    
// export default function PrivacyDropdown() {
//   const [sharing, setSharing] = useLocalStorage("sharing",'');

//   return (
//     <Select
//       data={[{ value: 'react', label: 'React library' }]}
//       value={sharing ? sharing.value : null}
//       onChange={(_value, option) => setSharing(option)}
//     />

//   );
// }

